name: Patch Addon Dialog Lint

on:
  pull_request:
    branches:
      - main
    types: [opened, reopened, synchronize]
    paths:
      - .qa/fix_addon_reducer_state.py
      - .github/workflows/patch-addon-dialog-lint.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: test/meal-builder-authenticated-crud-qa
          fetch-depth: 0
      - run: python .qa/fix_addon_reducer_state.py
      - name: Commit reducer state preservation
        run: |
          if git diff --quiet; then
            echo 'No patch needed.'
            exit 0
          fi
          git config user.name 'Basic Diet QA'
          git config user.email 'actions@users.noreply.github.com'
          git add src/components/pages/addons/AddonPlanDialog.tsx
          git commit -m 'fix(addons): preserve reducer filter state'
          git push origin HEAD:test/meal-builder-authenticated-crud-qa
